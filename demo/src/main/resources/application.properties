spring.application.name=Project2TierList

# Use the JAWSDB_URL provided by Heroku
spring.datasource.url=${JAWSDB_URL}
spring.datasource.username=${DB_USERNAME:rooty5q67sunmabfmjat}   
spring.datasource.password=${DB_PASSWORD:mflsa9rhit7ujblm} 
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate properties
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Ensure Spring Boot binds to the correct Heroku port
# server.port=${PORT}

# # Use JAWSDB_URL if present (Heroku), else fallback to local connection
# spring.datasource.url=${JAWSDB_URL:jdbc:mysql://localhost:3306/baukysxzy5qjlzdu}

# # Username & Password: Use Heroku's env vars, fallback to JAWSDB credentials for local
# spring.datasource.username=${DB_USERNAME:y5q67sunmabfmjat}
# spring.datasource.password=${DB_PASSWORD:mflsa9rhit7ujblm}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # Hibernate
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server port: use Heroku's PORT, default to 8080 locally
server.port=${PORT:8080}

# Enable detailed logging for database connections
logging.level.org.springframework.jdbc=DEBUG
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.hibernate.SQL=DEBUG